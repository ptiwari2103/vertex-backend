            body: JSON.stringify({
                user_id: user_id,
                annual_rate: annual_rate,
                payment_interval: payment_interval,
                amount: amount,
                duration: duration
            })
